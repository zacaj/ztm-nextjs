import { Card, CardBody, CardHeader } from "@chakra-ui/react";

export default async function() {
  return <Card>
    <CardHeader>New Tournament</CardHeader>
    <CardBody>
    </CardBody>
  </Card>;
}